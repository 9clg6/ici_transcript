name: core_foundation
description: Foundation layer - interfaces, config, logging, exceptions
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  rxdart: ^0.28.0

dev_dependencies:
  flutter_lints: ^6.0.0
