name: core_presentation
description: Presentation layer - theme, shared UI services
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.3

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
